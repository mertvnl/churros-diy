using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace Game.Enums 
{
    public enum CameraID
    {
        None = 0,
        CustomerCamera = 10,
        DrawingCamera = 20,
        FryingCamera = 30,
        ToppingCamera = 40
    }
}

